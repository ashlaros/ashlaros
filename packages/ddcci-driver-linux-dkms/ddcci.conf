# ddcci is an i2c driver: nothing autoloads it, and until it is loaded no
# monitor gets a /sys/class/backlight device. i2c-dev comes from ddcutil's
# own modules-load.d file and is what the bus is reached through.
ddcci
ddcci-backlight
