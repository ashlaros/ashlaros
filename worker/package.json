{
  "name": "ashlaros-web",
  "private": true,
  "type": "module",
  "scripts": {
    "game:sync": "cp src/game/logic.js ../docs/game/logic.js && cp src/game/quarry.js ../docs/game/quarry.js",
    "test": "npm run game:sync && node --test test/*.test.mjs",
    "deploy": "npm run game:sync && wrangler deploy"
  }
}
