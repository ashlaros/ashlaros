# The live session logs in as `live`, not root (#101): the desktop assumes a
# normal user - a home made from /etc/skel, a user session, a keyring - and
# the installer takes root through sudo when it needs it.
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --noreset --noclear --autologin live - ${TERM}
